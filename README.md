# App-Node.js</br>
0. Typescript</br>
1. таблица user - поля: (email, firstName, lastName, image(string))</br>
2. crud операции над user</br>
3. загрузка картинки на сервер</br>
4. создать post запрос на который генерируется pdf юзера который находится по email(передать в теле)</br>
5. далее функция находит пользователя в базе по email</br>
6. генерит pdf файл вида firstName + lastName + image</br>
7. сохраняет pdf файл в базу данных- возвращает пользователю результат в виде JSON (true/false)</br>
8. node fraemwork + orm + relation db</br>
